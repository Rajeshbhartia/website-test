- var inc = 0;

script.

	var handleCategory = (event) => {
		let currentTarget = event.currentTarget

		if(currentTarget.classList.contains('active')) return

		let activeItem = currentTarget.parentElement.parentElement.querySelector('.active')
		activeItem.classList.remove('active')
		currentTarget.classList.add('active')

		let categoryItem = document.getElementById('category-item')
		document.getElementById('category-name').innerText = currentTarget.innerText
		categoryItem.innerHTML = ""

		var dataObj = !{JSON.stringify(cwData)}
		console.log(dataObj)
		console.log(currentTarget.innerText)
		dataObj.forEach((item) => {
			//- item.submenu.forEach(tag=>{
			//- 	if(tag.label.toLowerCase() === currentTarget.innerText.toLowerCase()) {
			//- 		categoryItem.innerHTML += `<li><a href="${tag.path}">${tag.label}</a></li>`
			//- 	}
			//- })
			if(item.label.toLowerCase() === currentTarget.innerText.toLowerCase()){
				item.submenu.forEach(sm=>{
					categoryItem.innerHTML += `<li><a href="${sm.path}">${sm.label}</a></li>`
				})
			}
		})
	}

.document-category.py-5
	.container

		nav
			ol.breadcrumb.breadcrumb-custom
				li.breadcrumb-item
					a(href='/') Home
				li.breadcrumb-item
					a(href='/support') Support
				li.breadcrumb-item.text-capitalize.active(aria-current='page')= bodyData.name

		.row
			.col-md-4
				.single-category.shadow.py-4
					h5.pb-2.text-center CATEGORIES
					ul
						each item, index in cwData
							li
								a(href="javascript:;" onclick='handleCategory(event)' class=index === 0 ? "active": null)= item.label

			.col-md-5
				.box.py-4.ml-md-4
					h5#category-name.pb-2.text-capitalize= cwData[0].label
					ul
						#category-item
							each item in cwData[0].submenu
								li
									a(href= item.path)= item.label

			//- .col-md-3
			//- 	.box.py-4.ml-md-4
			//- 		h5.pb-2 Recently Added
			//- 		ul
			//- 			each item in docsResp
			//- 				if inc < 5
			//- 					li
			//- 						a(href= item.url)= item.name
			//- 				- inc++;
			//- 	.box.py-4.ml-md-4
			//- 		h5.pb-2 Most Popular Topics
			//- 		ul
			//- 			each item in popularPosts
			//- 				li
			//- 					a(href= item.url)= item.name

