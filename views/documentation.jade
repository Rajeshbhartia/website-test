- var inc = 0;

script.

	var handleCategory = (event) => {
		let currentTarget = event.currentTarget

		if(currentTarget.classList.contains('active')) return

		let activeItem = currentTarget.parentElement.parentElement.querySelector('.active')
		activeItem.classList.remove('active')
		currentTarget.classList.add('active')

		let categoryItem = document.getElementById('category-item')
		document.getElementById('category-name').innerText = currentTarget.innerText
		categoryItem.innerHTML = ""

		//- var categories = !{JSON.stringify(categories)}
		var allPostResp = !{JSON.stringify(allPostResp)}

		allPostResp.forEach((item) => {
			item.tags.forEach(tag=>{
				if(tag.toLowerCase().replace(/_/g," ") === currentTarget.innerText.toLowerCase()) {
					categoryItem.innerHTML += `<li><a href="${item.url_path+item.name}">${item.title}</a></li>`
				}
			})
		})
	}

.document-category.py-5
	.container

		nav
			ol.breadcrumb.breadcrumb-custom
				li.breadcrumb-item
					a(href='/') Home
				li.breadcrumb-item
					a(href='/support') Support
				li.breadcrumb-item.text-capitalize.active(aria-current='page')= bodyData.name

		.row
			.col-md-4
				.single-category.shadow.py-4
					h5.pb-2.text-center CATEGORIES
					ul
						each item, index in categories
							li
								a(href="javascript:;" onclick='handleCategory(event)' class=index === 0 ? "active": null)= item.name.replace(/_/g," ")

			.col-md-5
				.box.py-4.ml-md-4
					h5#category-name.pb-2.text-capitalize= categories[0].name
					ul
						#category-item
							each item in allPostResp
								each tag in item.tags
									if(tag === categories[0].name)
										li
											a(href= item.url_path+item.name)= item.title

			//- .col-md-3
			//- 	.box.py-4.ml-md-4
			//- 		h5.pb-2 Recently Added
			//- 		ul
			//- 			each item in docsResp
			//- 				if inc < 5
			//- 					li
			//- 						a(href= item.url)= item.name
			//- 				- inc++;
			//- 	.box.py-4.ml-md-4
			//- 		h5.pb-2 Most Popular Topics
			//- 		ul
			//- 			each item in popularPosts
			//- 				li
			//- 					a(href= item.url)= item.name

